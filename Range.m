global upper_limit low_limit
for j=1:dimension
    if f_number==1
        upper_limit(j)=5;
        low_limit(j)=-5;
    elseif f_number==2
        upper_limit(j)=2;
        low_limit(j)=-2;
    elseif f_number==3
        upper_limit(j)=5;
        low_limit(j)=-5;
    elseif f_number==4
        upper_limit(j)=5;
        low_limit(j)=-5;
    elseif f_number==5
        upper_limit(j)=100;
        low_limit(j)=-100;
    elseif f_number==13
        upper_limit(j)=5;
        low_limit(j)=-5;
    end
end